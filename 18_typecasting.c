#include <stdio.h>

int sum(int x, int y)
{
    return x + y;
}

int main(int argc, char const *agrv[])
{
    float b = 4;
    // Typecasting syntax
    // (data_type)value
    printf("%d", (int)b);
    return 0;
}